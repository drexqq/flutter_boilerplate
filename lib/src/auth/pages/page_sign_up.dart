import 'package:flutter/material.dart';

class PageSignUp extends StatelessWidget {
  const PageSignUp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Page Sign Up"),
    );
  }
}
